---
name: list-images
description: Browse recently generated images by project and limit
---

# List Images

Use this to discover recent outputs.

1. Optionally collect `projectId`.
2. Optionally collect `limit` (default 20, max 100).
3. Call `list_images`.
4. Return image list with key metadata in a concise format.
