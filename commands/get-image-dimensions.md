---
name: get-image-dimensions
description: Get recommended dimensions for common image use cases
---

# Get Image Dimensions

Use this before generation to select practical dimensions.

1. Collect `useCase` (hero, product, profile, card, thumbnail, social, logo, youtube, banner).
2. Call `get_image_dimensions`.
3. Return recommended width/height sets and format hints.
